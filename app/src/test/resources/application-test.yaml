
datasource.jdbc:
  enabled: true
  ref:
    - name: default-jdbc-client
      driver: h2
      username: sa
      password:
      protocol: mem
      database: "testdb"
      enabled: true
      load: "h2"

    - name: postgresql-1
      driver: postgresql
      username: todoservice
      password: password
      host: localhost
      port: 5432
      enabled: false
